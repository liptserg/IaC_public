#!/bin/bash

status_code=$(curl --insecure --write-out %{http_code} -s --cert /etc/patroni/ssl/server.crt --key /etc/patroni/ssl/server.key -o /dev/null https://127.0.0.1:8008/master)
if [[ "status_code" -ne 200 ]] ; then
  exit 1
else
  exit 0
fi

